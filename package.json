{
  "name": "omnitapprovals-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Unit + integration tests for the OmnitApprovals SuiteApp. Runs the SuiteScript 2.1 source via a minimal AMD shim under Node's built-in test runner.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  }
}
